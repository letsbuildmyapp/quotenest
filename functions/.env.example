# Required for real Resend delivery. If unset, the function logs a preview to the emulator console
# instead of sending. The frontend will still receive a successful response.
RESEND_API_KEY=
QUOTE_FROM_EMAIL=QuoteNest <onboarding@resend.dev>
